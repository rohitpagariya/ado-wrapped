# Azure DevOps Configuration
# Copy this file to .env and fill in your values

# Your Azure DevOps organization name (from https://dev.azure.com/{organization})
ADO_ORGANIZATION=your-organization-name

# The project name to analyze
ADO_PROJECT=your-project-name

# Personal Access Token with read permissions
# Create one at: https://dev.azure.com/{organization}/_usersSettings/tokens
# Required scopes: Code (Read), Work Items (Read), Build (Read), Graph (Read)
ADO_PAT=your-personal-access-token

# Optional: Year to analyze (defaults to current year)
# ADO_YEAR=2024

# Optional: Application port
# PORT=3000
