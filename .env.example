# Azure DevOps Wrapped - Environment Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!

# ============================================
# Auto-Configuration Feature
# ============================================
# If ALL required fields below are filled in, the app will:
# 1. Skip the landing page form
# 2. Automatically fetch and display stats
# 3. Use server-side PAT (more secure)
#
# If ANY required field is missing, the user will see the form
# and must enter their own PAT and configuration.

# ============================================
# Azure DevOps Connection (Required for Auto-Config)
# ============================================

# Your Azure DevOps organization name (from https://dev.azure.com/{organization})
ADO_ORGANIZATION=your-organization-name

# Repositories to analyze (format: project1/repo1,project2/repo2)
# Each repository is only queried within its specified project
# Examples:
#   Single repo: MyProject/my-repo
#   Multiple repos: ProjectA/frontend,ProjectA/backend,ProjectB/api
ADO_REPOSITORIES=your-project/your-repository

# Personal Access Token with read permissions
# Create one at: https://dev.azure.com/{organization}/_usersSettings/tokens
# Required scopes: Code (Read), Pull Request Threads (Read)
ADO_PAT=your-personal-access-token-here

# ============================================
# Filtering Options (Optional)
# ============================================

# Filter by specific user email (leave empty for all users)
ADO_USER_EMAIL=

# Year to analyze (defaults to current year)
ADO_YEAR=2025

# ============================================
# Feature Flags (Optional)
# ============================================

# Enable/disable specific data fetching
ADO_INCLUDE_COMMITS=true
ADO_INCLUDE_PULL_REQUESTS=true
ADO_INCLUDE_WORK_ITEMS=false
ADO_INCLUDE_BUILDS=false

# ============================================
# Application Settings (Optional)
# ============================================

# Development server port
PORT=3000

# Azure DevOps API version
ADO_API_VERSION=7.0

# ============================================
# Cache Settings (Optional)
# ============================================
# Enable file-based caching for Azure DevOps API responses.
# 
# Caching is DISABLED by default to avoid:
# - Disk storage accumulation in production environments
# - Memory pressure issues
#
# Set to 'true' for local development to speed up repeated requests.
# Leave unset or set to 'false' for production.
ADO_CACHE_ENABLED=true
